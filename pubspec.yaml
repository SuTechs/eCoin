name: ecoin
description: First freelancing project of ecoin wallets

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3
  country_code_picker: ^1.4.0
  flutter_sparkline: ^0.1.0 # for line graphs
  flutter_candlesticks: ^0.1.4 # for candle chart

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/images/crpto_icoins/
    - assets/images/settingIcons/



